# Twitter-Sentimental-Analysis
Built a Twitter sentiment analysis system using NLP and ML. Cleaned and processed tweets, applied TF-IDF, and trained models like Logistic Regression, SVM, Random Forest, and XGBoost. Used ensemble stacking and cross-validation for better accuracy. Deployed a joblib-based interface for real-time sentiment prediction.
